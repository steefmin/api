{
	"scripts": {
		"build-functions": "netlify-lambda build src",
		"dev": "npm run build-functions; netlify dev --port 3000",
		"test": "mocha"
	},
	"devDependencies": {
		"chakram": "^1.5.0",
		"mocha": "^8.1.3"
	},
	"dependencies": {
		"netlify-lambda": "^2.0.1"
	}
}
